import RedLight from "@/components/RedLight";
import React from "react";

const page = () => {
  return (
    <div style={{ all: "unset" }}>
      <RedLight />
    </div>
  );
};

export default page;
